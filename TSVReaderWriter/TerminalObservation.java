/**
 * TerminalObservation class to declare enum values. COUNT will be the option to
 * count how many matching records are found and MAX will be the option to find
 * the MAX String or byte value in the .tsv source file.
 * 
 * @author Alex Yu ajy2116
 */

public enum TerminalObservation {
	COUNT, MAX
}